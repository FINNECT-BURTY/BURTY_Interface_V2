spring.application.name=BURTY

# 로컬 기본 dev | 서버·Jenkins: SPRING_PROFILES_ACTIVE=prod
spring.profiles.active=${SPRING_PROFILES_ACTIVE:prod}

# 프로젝트 루트의 .env 파일을 properties로 로드 (없으면 무시)
spring.config.import=optional:file:./.env[.properties]
